import { HandlebarsPipe } from './handlebars.pipe';

describe('HandlebarsPipe', () => {
  it('create an instance', () => {
    const pipe = new HandlebarsPipe();
    expect(pipe).toBeTruthy();
  });
});
