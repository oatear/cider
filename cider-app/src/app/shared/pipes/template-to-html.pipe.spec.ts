import { CardToHtmlPipe } from './template-to-html.pipe';

describe('TemplateToHtmlPipe', () => {
  it('create an instance', () => {
    // const pipe = new CardToHtmlPipe();
    // expect(pipe).toBeTruthy();
  });
});
