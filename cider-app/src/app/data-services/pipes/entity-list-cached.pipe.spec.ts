import { EntityListCachedPipe } from './entity-list-cached.pipe';

describe('EntityListCachedPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityListCachedPipe();
    expect(pipe).toBeTruthy();
  });
});
