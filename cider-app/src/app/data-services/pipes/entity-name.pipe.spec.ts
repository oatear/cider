import { EntityNamePipe } from './entity-name.pipe';

describe('EntityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EntityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
