import { EntityListPipe } from './entity-list.pipe';

describe('EntityListPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityListPipe();
    expect(pipe).toBeTruthy();
  });
});
